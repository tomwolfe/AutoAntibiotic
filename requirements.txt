rdkit-pypi==2023.9.6
biopython==1.83
pandas==2.1.5
numpy==1.26.2
matplotlib==3.8.3
meeko==0.5.0
tqdm==4.66.2
sascore>=1.0.0
plotly>=5.18.0
scikit-learn>=1.3.0
joblib>=1.3.0
transformers>=4.30.0
torch>=2.0.0

# Optional: ML rescoring backends
# gnina  # install separately from https://github.com/gnina/gnina
# onnxruntime>=1.14.0  # for ONNX-based ADMET models

# Optional: MM-GB/SA rescoring
# openmm>=8.0.0  # install via: conda install -c conda-forge openmm
# pdbfixer>=1.9  # install via: conda install -c conda-forge pdbfixer
# ambertools>=23.0  # install via: conda install -c conda-forge ambertools
# parmed>=4.0.0  # for Amber topology handling

# Optional: water analysis
# biopython>=1.83  # already required; used for PDB parsing in water_analysis.py
